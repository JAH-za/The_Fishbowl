# Miscellaneous
*.class
*.lock
*.log
*.pyc
*.swp
.buildlog/
.history
# 🛑 The Veil Blocks — Files That Do Not Cross Over
node_modules/
.env
.DS_Store
/* GlobalStyles.css or inside styled-components */

@font-face {
  font-family: 'MoglanDemo';
  src: url('/fonts/MoglanDemo.ttf') format('truetype');
}

@font-face {
  font-family: 'MoglanTypeface';
  src: url('/fonts/MoglanTypeface.ttf') format('truetype');
}

@font-face {
  font-family: 'KugileRegular';
  src: url('/fonts/KugileRegular.ttf') format('truetype');
}

body {
  font-family: 'KugileRegular', serif;
}
import React from 'react';
import './GlobalStyles.css'; // assuming fonts are loaded here

const BookingCategories = () => {
  const categories = ['Overnight', 'Work function', 'Weekend Away'];

  return (
    <section style={{ padding: '2rem' }}>
      <h1 style={{ fontFamily: 'MoglanDemo', fontSize: '3rem' }}>
        Choose Your Escape
      </h1>
      <h2 style={{ fontFamily: 'MoglanTypeface', fontSize: '1.5rem', marginBottom: '1rem' }}>
        Booking Categories
      </h2>
      <ul style={{ listStyle: 'none', padding: 0 }}>
        {categories.map((cat, index) => (
          <li key={index} style={{
            fontFamily: 'KugileRegular',
            fontSize: '1.2rem',
            marginBottom: '0.5rem',
            borderBottom: '1px solid #ccc',
            paddingBottom: '0.5rem'
          }}>
            {cat}
          </li>
        ))}
      </ul>
    </section>
  );
};

export default BookingCategories;
import React from 'react';
import './GlobalStyles.css'; // includes your Moglan & Kugile fonts

const Homepage = () => {
  return (
    <main style={{
      backgroundColor: '#fff5f7',
      fontFamily: 'KugileRegular',
      minHeight: '100vh',
      padding: '3rem 2rem',
    }}>
      {/* Logo / Branding */}
      <header style={{ textAlign: 'center', marginBottom: '2rem' }}>
        <img 
          src="/assets/fishbowl-logo.png" 
          alt="The Fishbowl logo" 
          style={{ width: '120px', height: 'auto' }} 
        />
        <h1 style={{
          fontFamily: 'MoglanDemo',
          fontSize: '3.5rem',
          margin: '1rem 0 0.25rem',
        }}>
          The Fishbowl
        </h1>
        <p style={{
          fontFamily: 'MoglanTypeface',
          fontSize: '1.5rem',
          color: '#a75d72'
        }}>
          Presence. Rebellion. Connection.
        </p>
      </header>

      {/* Booking Portal */}
      <section style={{
        margin: '3rem auto',
        maxWidth: '600px',
        textAlign: 'center'
      }}>
        <h2 style={{
          fontFamily: 'MoglanTypeface',
          fontSize: '2rem',
          marginBottom: '1rem'
        }}>
          Choose Your Escape
        </h2>
        <ul style={{
          listStyle: 'none',
          padding: 0,
          fontSize: '1.2rem'
        }}>
          {['Overnight', 'Work function', 'Weekend Away'].map((type, i) => (
            <li key={i} style={{
              marginBottom: '0.75rem',
              padding: '1rem',
              backgroundColor: '#ffe2e8',
              borderRadius: '10px',
              cursor: 'pointer',
              transition: '0.3s',
            }}>
              {type}
            </li>
          ))}
        </ul>
      </section>

      {/* Footer / Ethos */}
      <footer style={{ textAlign: 'center', marginTop: '4rem', fontSize: '0.9rem' }}>
        <p>
          The Fishbowl is not a platform — it’s a portal. Curated companionship for those who move differently.
        </p>
      </footer>
    </main>
  );
};

export default Homepage;
import React from 'react';
import './GlobalStyles.css'; // includes your Moglan & Kugile fonts

const Homepage = () => {
  return (
    <main style={{
      backgroundColor: '#fff5f7',
      fontFamily: 'KugileRegular',
      minHeight: '100vh',
      padding: '3rem 2rem',
    }}>
      {/* Logo / Branding */}
      <header style={{ textAlign: 'center', marginBottom: '2rem' }}>
        <img 
          src="/assets/fishbowl-logo.png" 
          alt="The Fishbowl logo" 
          style={{ width: '120px', height: 'auto' }} 
        />
        <h1 style={{
          fontFamily: 'MoglanDemo',
          fontSize: '3.5rem',
          margin: '1rem 0 0.25rem',
        }}>
          The Fishbowl
        </h1>
        <p style={{
          fontFamily: 'MoglanTypeface',
          fontSize: '1.5rem',
          color: '#a75d72'
        }}>
          Presence. Rebellion. Connection.
        </p>
      </header>

      {/* Booking Portal */}
      <section style={{
        margin: '3rem auto',
        maxWidth: '600px',
        textAlign: 'center'
      }}>
        <h2 style={{
          fontFamily: 'MoglanTypeface',
          fontSize: '2rem',
          marginBottom: '1rem'
        }}>
          Choose Your Escape
        </h2>
        <ul style={{
          listStyle: 'none',
          padding: 0,
          fontSize: '1.2rem'
        }}>
          {['Overnight', 'Work function', 'Weekend Away'].map((type, i) => (
            <li key={i} style={{
              marginBottom: '0.75rem',
              padding: '1rem',
              backgroundColor: '#ffe2e8',
              borderRadius: '10px',
              cursor: 'pointer',
              transition: '0.3s',
            }}>
              {type}
            </li>
          ))}
        </ul>
      </section>

      {/* Footer / Ethos */}
      <footer style={{ textAlign: 'center', marginTop: '4rem', fontSize: '0.9rem' }}>
        <p>
          The Fishbowl is not a platform — it’s a portal. Curated companionship for those who move differently.
        </p>
      </footer>
    </main>
  );
};

export default Homepage;

import React from 'react';
import './GlobalStyles.css'; // Includes MoglanDemo, MoglanTypeface, KugileRegular

const OnboardingFlow = () => {
  return (
    <div style={{
      backgroundColor: '#fceeee',
      minHeight: '100vh',
      fontFamily: 'KugileRegular',
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      justifyContent: 'center',
      padding: '3rem 2rem',
    }}>
      {/* Step 1: Welcome */}
      <h1 style={{
        fontFamily: 'MoglanDemo',
        fontSize: '3rem',
        color: '#8c3b4a',
        marginBottom: '1rem'
      }}>
        Welcome to The Fishbowl
      </h1>
      <p style={{
        fontFamily: 'MoglanTypeface',
        fontSize: '1.5rem',
        color: '#b2606d',
        textAlign: 'center',
        maxWidth: '500px'
      }}>
        Presence wrapped in intimacy. Choose how you want to feel.
      </p>

      {/* Step 2: Mood Selector */}
      <div style={{ margin: '3rem 0', width: '100%', maxWidth: '600px' }}>
        {['Intentional connection', 'Bold companionship', 'Serene escape'].map((mood, i) => (
          <button key={i} style={{
            display: 'block',
            width: '100%',
            padding: '1rem',
            marginBottom: '1rem',
            border: 'none',
            borderRadius: '10px',
            backgroundColor: '#ffe2e8',
            color: '#8c3b4a',
            fontSize: '1.2rem',
            fontFamily: 'KugileRegular',
            cursor: 'pointer',
            transition: '0.3s'
          }}>
            {mood}
          </button>
        ))}
      </div>

      {/* Final Portal CTA */}
      <button style={{
        padding: '1rem 2rem',
        backgroundColor: '#8c3b4a',
        color: '#fff',
        fontSize: '1.2rem',
        fontFamily: 'MoglanTypeface',
        borderRadius: '20px',
        border: 'none',
        boxShadow: '0 4px 10px rgba(0,0,0,0.2)',
        cursor: 'pointer'
      }}>
        Slip Beneath the Surface
      </button>
    </div>
  );
};

export default OnboardingFlow;

<p style={{ fontStyle: 'italic', fontSize: '0.9rem' }}>
  Seeker — "I arrive open, craving truth."
</p>
<p style={{ fontStyle: 'italic', fontSize: '0.9rem' }}>
  Guest Companion — "I offer warmth and presence."
</p>
<p style={{ fontStyle: 'italic', fontSize: '0.9rem' }}>
  Host — "I curate space, intention, and ease."
</p>
import React, { useState } from 'react';
import './GlobalStyles.css'; // Includes MoglanDemo, MoglanTypeface, KugileRegular

const FishbowlOnboarding = () => {
  const [identity, setIdentity] = useState('');
  const [escape, setEscape] = useState('');

  return (
    <main style={{
      backgroundColor: '#fceeee',
      minHeight: '100vh',
      fontFamily: 'KugileRegular',
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      padding: '3rem 2rem',
    }}>
      {/* 🪷 Step 1: Welcome */}
      <header style={{ textAlign: 'center', marginBottom: '2rem' }}>
        <img src="/assets/fishbowl-logo.png" alt="Fishbowl Logo" style={{ width: '100px' }} />
        <h1 style={{
          fontFamily: 'MoglanDemo',
          fontSize: '3rem',
          color: '#8c3b4a'
        }}>
          Welcome to The Fishbowl
        </h1>
        <p style={{
          fontFamily: 'MoglanTypeface',
          fontSize: '1.4rem',
          color: '#b2606d'
        }}>
          Where intimacy meets intention.
        </p>
      </header>

      {/* 🪪 Step 2: Booking Identity */}
      <section style={{ marginBottom: '2rem', textAlign: 'center' }}>
        <h2 style={{
          fontFamily: 'MoglanTypeface',
          fontSize: '1.8rem',
          marginBottom: '1rem'
        }}>
          How would you like to enter?
        </h2>
        {['Seeker', 'Guest Companion', 'Host'].map((role, index) => (
          <button
            key={index}
            onClick={() => setIdentity(role)}
            style={{
              padding: '1rem',
              margin: '0.5rem',
              borderRadius: '10px',
              border: identity === role ? '2px solid #8c3b4a' : '1px solid #ccc',
              backgroundColor: identity === role ? '#ffe2e8' : '#fff',
              fontFamily: 'KugileRegular',
              cursor: 'pointer'
            }}
          >
            {role}
          </button>
        ))}
      </section>

      {/* 🌙 Step 3: Choose Your Escape */}
      <section style={{ textAlign: 'center' }}>
        <h2 style={{
          fontFamily: 'MoglanTypeface',
          fontSize: '1.8rem',
          marginBottom: '1rem'
        }}>
          Choose Your Escape
        </h2>
        {['Overnight', 'Work function', 'Weekend Away'].map((option, index) => (
          <button
            key={index}
            onClick={() => setEscape(option)}
            style={{
              padding: '1rem',
              margin: '0.5rem',
              borderRadius: '10px',
              border: escape === option ? '2px solid #8c3b4a' : '1px solid #ccc',
              backgroundColor: escape === option ? '#ffe2e8' : '#fff',
              fontFamily: 'KugileRegular',
              cursor: 'pointer'
            }}
          >
            {option}
          </button>
        ))}
      </section>

      {/* 🎟 Final CTA */}
      {identity && escape && (
        <button style={{
          marginTop: '3rem',
          padding: '1rem 2rem',
          backgroundColor: '#8c3b4a',
          color: '#fff',
          borderRadius: '20px',
          fontFamily: 'MoglanTypeface',
          fontSize: '1.2rem',
          border: 'none',
          cursor: 'pointer'
        }}>
          Slip Beneath the Surface
        </button>
      )}
    </main>
  );
};

export default FishbowlOnboarding;
<img src="/assets/fishbowl-logo.png" alt="Fishbowl Logo" />
"C:\Users\jason\OneDrive\Fishbowl\Logo's\FBLogo.png"

# Flutter repo-specific
/bin/cache/
/bin/internal/bootstrap.bat
/bin/internal/bootstrap.sh
/bin/mingit/
/dev/benchmarks/mega_gallery/
/dev/bots/.recipe_deps
/dev/bots/android_tools/
/dev/devicelab/ABresults*.json
/dev/docs/doc/
/dev/docs/flutter.docs.zip
/dev/docs/lib/
/dev/docs/pubspec.yaml
/dev/integration_tests/**/xcuserdata
/dev/integration_tests/**/Pods
/packages/flutter/coverage/
version
analysis_benchmark.json

# packages file containing multi-root paths
.packages.generated

# Flutter/Dart/Pub related
**/doc/api/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
**/generated_plugin_registrant.dart
.packages
.pub-preload-cache/
.pub/
build/
flutter_*.png
linked_*.ds
unlinked.ds
unlinked_spec.ds

# Android related
**/android/**/gradle-wrapper.jar
.gradle/
**/android/captures/
**/android/gradlew
**/android/gradlew.bat
**/android/local.properties
**/android/**/GeneratedPluginRegistrant.java
**/android/key.properties
*.jks

# iOS/XCode related
**/ios/**/*.mode1v3
**/ios/**/*.mode2v3
**/ios/**/*.moved-aside
**/ios/**/*.pbxuser
**/ios/**/*.perspectivev3
**/ios/**/*sync/
**/ios/**/.sconsign.dblite
**/ios/**/.tags*
**/ios/**/.vagrant/
**/ios/**/DerivedData/
**/ios/**/Icon?
**/ios/**/Pods/
**/ios/**/.symlinks/
**/ios/**/profile
**/ios/**/xcuserdata
**/ios/.generated/
**/ios/Flutter/.last_build_id
**/ios/Flutter/App.framework
**/ios/Flutter/Flutter.framework
**/ios/Flutter/Flutter.podspec
**/ios/Flutter/Generated.xcconfig
**/ios/Flutter/ephemeral
**/ios/Flutter/app.flx
**/ios/Flutter/app.zip
**/ios/Flutter/flutter_assets/
**/ios/Flutter/flutter_export_environment.sh
**/ios/ServiceDefinitions.json
**/ios/Runner/GeneratedPluginRegistrant.*

# macOS
**/Flutter/ephemeral/
**/Pods/
**/macos/Flutter/GeneratedPluginRegistrant.swift
**/macos/Flutter/ephemeral
**/xcuserdata/

# Windows
**/windows/flutter/generated_plugin_registrant.cc
**/windows/flutter/generated_plugin_registrant.h
**/windows/flutter/generated_plugins.cmake

# Linux
**/linux/flutter/generated_plugin_registrant.cc
**/linux/flutter/generated_plugin_registrant.h
**/linux/flutter/generated_plugins.cmake

# Coverage
coverage/

# Symbols
app.*.symbols

# Exceptions to above rules.
!**/ios/**/default.mode1v3
!**/ios/**/default.mode2v3
!**/ios/**/default.pbxuser
!**/ios/**/default.perspectivev3
!/packages/flutter_tools/test/data/dart_dependencies_test/**/.packages
!/dev/ci/**/Gemfile.lock
